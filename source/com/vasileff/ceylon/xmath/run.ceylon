"Run the module `com.vasileff.ceylon.xmath`." 
shared void run(){
    // Write your code here and afterwards
    // on the command line compile with "ceylon compile com.vasileff.ceylon.xmath" or "ant compile"
    // and execute with "ceylon compile com.vasileff.ceylon.xmath/0.0.1" or "ant run"
}
